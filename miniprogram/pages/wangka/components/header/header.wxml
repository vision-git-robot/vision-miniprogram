<view class="header">
  <image
    mode="widthFix"
    class="header-img"
    src="{{tools.renderUrl(url)}}"
    style="background-color: {{bgColor || 'none'}}"
    lazy-load
  />
</view>

<wxs src="../../utils/tools.wxs" module="tools"></wxs>
